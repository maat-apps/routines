export { cn } from "cn";

// Shared by every button that floats fixed over page content (reset,
// reset all, new routine) so they read as one consistent "elevated" style.
export const FIXED_ACTION_SHADOW = "shadow-[0_8px_22px_oklch(0_0_0_/_28%)]";
